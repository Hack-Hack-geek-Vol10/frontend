# src/mutations/UpdateProject.graphql

mutation UpdateProject($projectId: ID!, $title: String, $lastImage: Upload) {
  updateProject(projectId: $projectId, title: $title, lastImage: $lastImage) {
    projectId
    title
    lastImage
    isPersonal
    updatedAt
  }
}
